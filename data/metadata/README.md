# Data Metadata

This folder stores machine-readable metadata about datasets and curation choices.

Current file:

- `qsar_biodegradation_metadata.json`: dataset version, row count, descriptor count, target labels, representation limits, and curation decisions

Use this folder when writing dataset and limitations sections for a report or paper.
