# Raw Data

This folder stores original source datasets.

Current file:

- `qsar_biodegradation.csv`: source QSAR biodegradation dataset used as the current working dataset

Do not manually edit raw data files. If a cleaned or standardized version is needed, generate it into `data/processed/`.
