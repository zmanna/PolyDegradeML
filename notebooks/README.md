# Notebooks

This folder is for notebooks that support exploration, prototyping, or visual inspection.

Notebooks are not the canonical source of reproducible results. If a notebook produces a result that should be cited or included in the final report, convert the logic into `src/` modules and `scripts/` workflows.

See `exploratory/` for scratch or historical notebooks.
